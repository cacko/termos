# https://taskfile.dev

version: '3'

vars:
  PROJECT: termos
  ENTRY: src/termos.py
  APP: termos
  WORKDIR: src/

tasks:
  default:
    cmds:
      - direnv exec . mamba run -n {{.PROJECT}} python {{.ENTRY}} {{.CLI_ARGS}}
    silent: true

  logs:
    desc: logs
    cmds:
      - tail -f ~/Library/Logs/net.cacko.termoservice/termosvc.log 

  export:
    desc: export env
    cmds:
      - direnv exec . mamba env export --no-builds -n {{.PROJECT}} > environment.yml

  start:
    desc: start
    cmds:
      - sudo launchctl start net.cacko.termoservice

  stop:
    desc: stop
    cmds:
      - sudo launchctl stop net.cacko.termoservice


  restart:
    desc: stop
    cmds:
      - sudo launchctl stop net.cacko.termoservice
      - sleep 10
      - sudo launchctl start net.cacko.termoservice

  push:
    desc: commit and pushes current changes
    cmds:
      - git add .  || true
      - gcom || true
      - git push || true

  pull:
    desc: pulls from git
    cmds:
      - ssh $REMOTE_HOST 'source ~/.zshrc;cd /var/www/{{.PROJECT}};git reset --hard;git pull'

  pp:
    desc: push and pull
    cmds:
      - git add .  || true
      - gcom || true
      - git push || true
      - git pull || true

  run:
    desc: run
    dir: $WORKDIR
    cmds:
      - mamba run -n {{.PROJECT}} python {{.ENTRY}}

  build:
    desc: build app
    cmds:
      - rm -rf {{.APP}}.build
      - rm -rf {{.APP}}.dist
      - mamba run -n {{.PROJECT}} python -m nuitka {{.ENTRY}}